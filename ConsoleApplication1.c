#include <stdio.h>
#include<limits.h>

int main(void)
{
	{double m;
	size_t m = sizeof(int);
	long x;
	size_t m = sizeof(x);
	size_t m = sizeof('5')
	}
	double a;
	scanf("%lf", &a);
	a < 100;
	printf("%lf %lf %lf", a, a, a);
	printf("\n%lf %lf %lf", a, a, a);
	printf("\n%lf %lf %lf", a, a, a);
	return 0;
	
}
