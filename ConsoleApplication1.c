#include <stdio.h>
#include<limits.h>

int main(void)
{
	{double m;
	size_t m = sizeof(int);
	long x;
	size_t m = sizeof(x);
	size_t m = sizeof('5')
	}
	double a;
	a < 100;
	scanf("%lf", &a);
	printf("%lf ----- %lf", a, a, a);
	printf("\n|%lf|", a);
	printf("\n%lf ----- %lf", a, a);
	return 0;
}
